
version: "3.4"

services:
  client:
    stdin_open: true 
    build: 
      context: ./client
      dockerfile: DockerFile
    environment:
      NODE_PATH: src
    expose:
      - 3000
    ports:
      - 3000:3000
    volumes:
      - ./client/src:/app/src
    command: npm start
    